# data/el-dorado/

`rulings.parquet` — one row per ruling extracted from PDFs in `archive/el-dorado/`. Schema in `schema/ruling.py`. Generated by `python -m ingest.orchestrate`.

Quick query:

```bash
python -c "import duckdb; print(duckdb.sql(\"SELECT outcome, COUNT(*) FROM 'data/el-dorado/rulings.parquet' GROUP BY 1 ORDER BY 2 DESC\"))"
```
